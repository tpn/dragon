name: call-py313-runner-build-aarch64

#
# Call runner-build.yaml for a python 3.13 aarch64 dst-workflow or diy-workflow build
#

on:
  push:
    branches: # build for python 3.13 aarch64 ONLY on pushes to branches below
      - 'develop'
      - 'v0*branch' # catch release branches
      - 'main'

jobs:
  call-build:
    uses: ./.github/workflows/runner-build.yaml
    secrets: inherit
    with:
      build-command: "dst/dragon_build.sh -n -p 313" # dst example = "dst/dragon_build.sh -n -p 313" or diy example = "runBuildPrep.py313.sh"
      build-type: "use-hybrid-workflow" # options = use-dst-workflow use-hybrid-workflow use-diy-workflow
      build-container: "arti.hpc.amslabs.hpecorp.net/quay-remote/pypa/manylinux_2_28_aarch64"
      build-cpu: "arm" # options = "x86" or "arm"
